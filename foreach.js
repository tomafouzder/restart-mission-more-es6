const numbers = [1, 2, 3, 4, 5, 6, 7];
// numbers.forEach(x => console.log(x));  // 1 2 3 4 5 6 7 
numbers.forEach(num => {
    const doubled = num * 2;
    console.log(doubled);
})

// Note: map return kore but forEach return / result dibe na
